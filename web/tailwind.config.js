// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      colors: {
        primary: '#4d6f85',       // azul acinzentado (principal)
        secondary: '#cb5226',     // laranja queimado (destaque/CTA)
        neutralLight: '#f4f2ee',  // fundo claro
        neutralDark: '#292223',   // texto escuro
        accent: '#e18b51',        // dourado suave (hover/detalhes)
        accentLight: '#8ca5b5',   // azul claro (apoio/harmonização)
      },
    },
  },
};
